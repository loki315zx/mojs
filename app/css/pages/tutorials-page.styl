@import '../imports'
@import '../blocks/motion-lettering'

sidebarWidth = 20
{el = '.tutorials-page'}
  // height        200*gs
  padding-left  7.5*gs
  padding-top   18.5*gs
  &__content
    position    relative
    margin-left sidebarWidth*gs
    .motion-lettering
      fly()
  &__sidebar
    width       sidebarWidth*gs
    float       left
    min-height  100%
    // position    fixed
  .tutorials-sidebar
    fly()
  .post
    max-width   80*gs
    fly()
 